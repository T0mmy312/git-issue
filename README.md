# git-issue

This project aims to add simple issue management with issue commit linking like in github locally to git.

## Usage

To use this Project the compiled binary `git-issue` must simply be added to your path and then it can be used by doing `git issue --help` for example.